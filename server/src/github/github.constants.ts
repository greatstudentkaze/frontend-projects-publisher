export const ORG_NAME = 'ustudy-org';
export const ORG_URL = `https://github.com/${ORG_NAME}`;
export const ORG_GH_PAGES_URL = `https://${ORG_NAME}.github.io`;

export const CLONED_REPOSITORIES_FOLDER_NAME = 'cloned-repositories';
export const CLONED_REPOSITORIES_FOLDER_PATH = `./${CLONED_REPOSITORIES_FOLDER_NAME}`;
export const BUILD_PROJECT_DIRECTORY = 'dist';

export const SUCCESS_DEPLOY_MESSAGE = 'Успешно опубликовано';

const API_ROOT_URL = 'https://api.github.com';

export const API_URL = {
    repositories: `${API_ROOT_URL}/repos`,
}